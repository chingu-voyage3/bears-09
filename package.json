{
  "name": "bears-09",
  "version": "1.0.0",
  "scripts": {
    "install": "(cd client && npm install) && (cd server && npm install)",
    "start":
      "concurrently \"cd client && npm run start\" \"cd server && npm run start\""
  },
  "dependencies": {
    "concurrently": "^3.5.1"
  }
}
